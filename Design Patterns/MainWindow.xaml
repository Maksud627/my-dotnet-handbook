<Window x:Class="DesignPatterns.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesignPatterns"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Button Height="50" Width="100" Grid.Row="0" x:Name="btnLogger" Click="Button_Click" Content="Log Message" Style="{StaticResource InformButton}"></Button>
        <Button Height="50" Width="100" Grid.Row="1" x:Name="btnFactory" Click="btnFactory_Click" Content="Factory Method" Style="{StaticResource InformButton}"></Button>
        <Button Height="50" Width="100" Grid.Row="2" x:Name="btnBuilder" Click="btnBuilder_Click" Content="Builder Pattern" Style="{StaticResource InformButton}"></Button>

    </Grid>
</Window>
